name: pipeline overly broad
on: { pull_request: { types: [opened] } }
jobs:
  example:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: echo "contents:write not allowed outside allowlist"
