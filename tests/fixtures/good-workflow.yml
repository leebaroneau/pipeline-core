name: pipeline good example
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  example:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v5
      - run: node scripts/pipeline/check-branch-name.mjs
