# Starter Issue Templates

These templates are placeholders carrying lee-dashboard's domain/component labels. After you create your own `.github/pipeline-config.yml`, run `make pipeline-generate` to regenerate these templates with YOUR domain/component labels.

Until then, these starter templates work as a baseline — issues opened with them will be labelled with placeholders that won't match any of your declared labels. Regenerate before going live.
