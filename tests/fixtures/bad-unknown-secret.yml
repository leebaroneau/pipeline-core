name: pipeline unknown secret
on: { pull_request: { types: [opened] } }
jobs:
  example:
    runs-on: ubuntu-latest
    permissions: { contents: read }
    env:
      SOME_KEY: ${{ secrets.NOT_GITHUB_TOKEN }}
    steps:
      - run: echo "uses non-GITHUB_TOKEN secret"
