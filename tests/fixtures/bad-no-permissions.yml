name: pipeline missing permissions
on:
  pull_request:
    types: [opened]
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "no permissions block"
